<Page
    x:Class="RetailMobile.Presentation.Views.OrderConfirmationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RetailMobile.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:RetailMobile.Helpers"
    xmlns:toolkit="using:Uno.Toolkit.UI"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
    </Page.Resources>

    <Grid toolkit:SafeArea.Insets="Top, Bottom"
          Padding="20, 0, 20, 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              Margin="0"
              Padding="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Border Width="80" 
                        Height="80" 
                        CornerRadius="50"
                        Background="Green"
                        Visibility="{Binding IsSuccess, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <SymbolIcon Symbol="Accept"
                                Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" 
                                Visibility="{Binding IsSuccess, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Border>
                <Border Width="80" 
                        Height="80" 
                        CornerRadius="50"
                        Background="#F83758"
                        Visibility="{Binding IsSuccess, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                    <SymbolIcon Symbol="Clear"
                                Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                                Visibility="{Binding IsSuccess, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                </Border>
            </Grid>

            <TextBlock Grid.Row="1"
                       Text="{Binding Message}"
                       FontSize="20"
                       FontWeight="SemiBold"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,20,0,0"
                       Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
        </Grid>

        <Button Grid.Row="1"
                Content="Về trang chủ"
                Command="{Binding NavigateToProductListPageCommand}"
                HorizontalAlignment="Stretch"
                Style="{StaticResource AccentButtonStyle}"
                Background="#F83758"
                CornerRadius="8"
                Height="48"
                Margin="0, 0, 0, 20"/>
    </Grid>
</Page>
